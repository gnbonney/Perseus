comment Multi-file namespace library sample: first file;
namespace mylib.shapes;

class Point;
begin
    real x, y;

    procedure set(nx, ny);
        value nx, ny;
        real nx, ny;
    begin
        x := nx;
        y := ny
    end
end
