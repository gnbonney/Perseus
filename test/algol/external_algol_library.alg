comment Separately compiled Algol library for Milestone 26A external procedure tests;
begin
    real procedure hypot2(a, b);
        value a, b;
        real a, b;
        hypot2 := sqrt(a * a + b * b)
end
